h1 Listing deployments

table
  thead
    tr
      th Provider
      th Region
      th Flavor
      th
      th
      th

  tbody
    - @deployments.each do |deployment|
      tr
        td = t("providers.#{deployment.provider_type}")
        td = deployment.region
        td = deployment.flavor
        td = link_to 'Show', deployment
        td = link_to 'Destroy', deployment, data: {:confirm => 'Are you sure?'}, :method => :delete

br

- if @template.present?
  = link_to 'New Deployment', new_template_deployment_path(@template)
