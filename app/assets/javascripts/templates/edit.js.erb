//= require templates
//= require bootstrap/modal
//= require templates/arguments
//= require select2
//= require tags_list
//= require_self

;(function($) {
  var tags_list, template_arguments;

  tags_list = new SiteFull.TagsList('.tags-list select', { placeholder: '<%= I18n.t('simple_form.placeholders.template.tag_list') %>' });

  template_arguments = new SiteFull.TemplateArguments();

  $(document).on('ready', function() {
      tags_list.render();
      template_arguments.init();
  });
})(jQuery);
